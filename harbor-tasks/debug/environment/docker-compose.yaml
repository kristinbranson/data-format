services:
  main:
    volumes:
      - ../../../data/sosa2024:/app/data:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
