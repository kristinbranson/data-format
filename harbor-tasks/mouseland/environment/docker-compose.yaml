services:
  main:
    volumes:
      - ../../../data/mouseland:/app/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
