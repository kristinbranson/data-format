services:
  main:
    volumes:
      - "${DATA_ROOT:?DATA_ROOT must be set - run via harbor-scripts/run_harbor.sh}/allen2p/visual-behavior-ophys-1.1.0:/app/data/visual-behavior-ophys-1.1.0:ro"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
