services:
  main:
    volumes:
      - ../../../data/mouseland:/app/data:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
