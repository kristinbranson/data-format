services:
  main:
    volumes:
      - ../../../data/lee2025:/app/data:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
