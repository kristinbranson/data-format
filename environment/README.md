# Conda environment yaml files

- `minimal_environment.yaml`: Posthoc attempt to recreate conda environment with all dependencies necessary for code
- `decoder-data-format_freeze_full_environment.yaml`: Freeze of incrementally built environment used while testing `decoder.py`
- `eval-data-format_freeze_full_environment.yaml`: Freeze of incrementally build environment used while running all harbor scripts